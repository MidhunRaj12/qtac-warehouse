services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: qtac
      POSTGRES_PASSWORD: qtac
      POSTGRES_DB: qtac
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
